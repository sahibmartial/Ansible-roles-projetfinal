#Roles
```bash
git clone this repo 
=> in your folder roles create a file requirements.yml
=> in this file specify the link of the repo contain your role
=> ansible-galaxy install -r reaquirements.yml :download roles on local 
=> edit ansible.cfg on your project put your local config
```
